# eb development website
**IN PHP**

## Dev
Basic:
```bash
php -S localhost:3000
```

Docker:
```bash
# See https://hub.docker.com/_/php for more info
docker build -t my-php-app .
docker run -it --rm --name my-running-app my-php-app
```
